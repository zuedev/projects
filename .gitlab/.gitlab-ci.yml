uag_site_deploy:
  image: docker:20
  services:
    - docker:20-dind
  variables:
    CONTAINER_IDENTIFIER: zuedev/monorepo/www.unnamed.group
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY/$CONTAINER_IDENTIFIER:latest ./$CONTAINER_IDENTIFIER
    - docker push $CI_REGISTRY/$CONTAINER_IDENTIFIER:latest
  only:
    refs:
      - master
    changes:
      - active/www.unnamed.group/**/*
